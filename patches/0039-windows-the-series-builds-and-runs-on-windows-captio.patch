From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Yevhen Salitrynskyi <ysalitrynskyi@gmail.com>
Date: Tue, 8 Sep 2026 09:30:00 -0400
Subject: windows: the series builds and runs on Windows -- caption buttons,
 the row's colour, the rail

The first build of the series for a non-macOS target (docs/ARC-ROUND2.md,
round 8; M8 in docs/ROADMAP.md had not started). Nine portability errors in
four files, all one cause, and then the operator's look at the result --
five things, two more found while capturing them -- across the layout, the
address row's colour, the sidebar's rail and the expand-on-hover overlay:

- base::FilePath is std::wstring on Windows and std::string on the Mac, so
  every narrow-string Append("...") and FilePath("...") in the Arc importers,
  the sidebar backups and the screenshot file name failed to compile.
  FILE_PATH_LITERAL, FromUTF8Unsafe, AppendASCII and AsUTF8Unsafe, as
  Chromium's own code spells them; nothing changes on the Mac.
- Windows' caption buttons are frame-owned controls at the top RIGHT, over
  the address row's column, and the row ran under them. macOS's traffic
  lights sit over the sidebar's column, which is why the layout skipped the
  exclusion with a vertical strip. The row now stops short of the frame's
  trailing exclusion, the card's exposed top-right corner rounds, and with
  the row hidden (toolbar T8) the empty top container keeps the caption
  strip's height so the card cannot cover the buttons -- from the rail the
  window could not be closed at all. With no leading exclusion the sidebar's
  toggle takes the container's baseline instead of a centre computed from an
  empty box.
- Focused, the location bar borrows the results dropdown's background
  (LocationBarView::GetBackgroundColor while the caret shows), which the
  page-bar colour supplier never set: near-black on a white page's row. The
  dropdown's background, text, icons and selection follow the page too, as
  Safari's do, in the bar's contrast colour.
- A reload dropped the row to the window ground and left it there: the new
  Page starts with no colour, and WebContentsImpl announces a background
  colour only when it differs from the last one it SENT -- kept on the
  WebContents, not the Page -- so white reloading to white is never
  announced. The controller keeps its colour while the page loads and
  re-reads the Page at first paint and at the end of loading.
- Expanded on hover the strip floats over the page as a layer that does not
  fill its bounds opaquely, with a background made invisible so the window's
  gradient shows through (HANDOFF trap 8): every row read through to the
  page. The background is visible exactly while the strip is collapsed by
  state and drawn wider than the rail.
- The rail (sidebar Y6): the toggle, the Archived row and the New Tab row
  centre in the rail and take a size up, the Archived row as its icon alone;
  the Space switcher stacks its chips in a column with no spacers instead of
  a row that ran out of the rail and drew its "+" over the page; the
  downloads button and the switcher stack too. All keyed on the width being
  drawn, not the collapse state: expanded on hover the state stays collapsed
  while the rows are at the open width, and the first cut centred the toggle
  in a 350 DIP overlay.

The operator's reply the same afternoon, and one thing found on the way:

- The rail's tab pill was still 26 DIP wide, the "+" above it clipped to
  10 DIP by its own inset, the toggle 3.5 DIP right of the rows' centre.
  The width a rail row gets is decided in UnpinnedTabContainerViewLayout,
  not TabStripViewLayout, which is why overriding the strip's padding had
  changed nothing. Every container's side padding is stedding::
  kRailRowPadding, 3 DIP, in the rail -- keyed on the width being laid
  out -- and TabView::CollapsedWidth says the same 44, so a rail row is a
  44 DIP square around a 22 DIP favicon centred in the rail plus the
  gutter. The "+ New Tab" row takes the rail form from the region view
  (its plus alone, no inset), and the button containers sit on the same
  column (sidebar Y8; S-53).
- In the rail there was no address bar at all: the row hidden with the
  sidebar (toolbar T8) left an empty band under the caption buttons. The
  rule keeps the row while the frame's own buttons sit over its column
  (the trailing exclusion): the strip is there whatever the sidebar does.
  The toggle still wins; macOS is unchanged (toolbar T22, a unit test).
- A profile killed while the sidebar was collapsed never opened again:
  the crashed-session "Restore pages?" bubble anchors to the app menu
  button before the window is shown, the button is not drawn with the row
  hidden, and the fallback anchor is a tracked element the tracker does
  not know until the widget is visible -- CHECK, every launch. The button
  anchors such a bubble to the top container instead (toolbar T23).
- The bar stopped with the row's contents, short of the caption buttons,
  and its end read as a cut corner beside them (operator, second reply).
  The bar runs on under the buttons to the card's edge; the contents
  still stop short; the card's top-right corner rounds as on the Mac;
  the buttons' glyphs take the bar's contrast colour
  (kColorCaptionButtonOnToolbar from PageBarColorSupplier); and the
  address field centres on the bar, the layout handing the toolbar the
  reserve (toolbar T19, T18).
- Found while looking at that across window sizes: in a 900 DIP window
  with the sidebar open the field sat over the back, forward and reload
  buttons. Round 7's centring read the two spacers' bounds after the
  layout had dropped them for want of width -- hidden, at the origin --
  and clamped the field to x 0. It keeps the layout's position when the
  spacers are gone (toolbar T18).
- An "Action required" chip on the app menu: Chromium's ExternalRegistryLoader
  reads Google Chrome's own registry key whatever the branding, and
  Adobe's installer had put its Acrobat extension there, so the browser
  was fetching it from Google's update server and asking. The provider
  is not created on Windows (privacy Q9). The chip, for the errors that
  are real, is the toolbar buttons' height (toolbar T25).
- The caption buttons stretched from the window's top edge to the frame's
  height, their centre 4 DIP above the row's; they centre on the row's
  centre line at their own height (toolbar T19).

Measured on the Windows build against the mac probes (tooling/probes/
window.json at scale 1): 20 of 23 pass, the three others being state or
platform -- no essentials in a fresh profile, and the probe that expects the
row where Windows now has the caption strip. The card's edges and gutters
and its 12.00 DIP corner radius are the mac's to the pixel.

What this is not: a port. Branding is mac-gated, and the keyboard map, the
Cocoa menus and the keychain naming live in is_mac files.

One patch across features rather than fixups, as 0013 and 0037 were for
rounds 5 and 7: these hunks sit on lines several later patches rewrote.

Why: docs/ARC-ROUND2.md round 8, the operator's look at the first Windows
build (2026-09-08).
Removable when: M8 rebuilds the sidebar and the page card on upstream's
vertical tabs with the frame's exclusions handled there, and Chromium's own
importers and backups are used in place of ours.
---
 .../extensions/external_provider_impl.cc      |  15 ++-
 chrome/browser/ui/layout_constants.cc         |  13 ++-
 chrome/browser/ui/spaces/arc_data_importer.cc |  14 +--
 .../browser/ui/spaces/arc_sidebar_importer.cc |  10 +-
 chrome/browser/ui/spaces/sidebar_backup.cc    |  16 ++-
 chrome/browser/ui/stedding/address_row.cc     |   1 +
 chrome/browser/ui/stedding/address_row.h      |   7 ++
 .../browser/ui/stedding/address_row_rules.cc  |   3 +-
 .../browser/ui/stedding/address_row_rules.h   |   4 +
 .../ui/stedding/address_row_rules_unittest.cc |  13 +++
 .../stedding/screenshot/screenshot_capture.cc |   2 +-
 .../stedding/theme/page_bar_color_supplier.cc |  27 +++++
 .../theme/page_bar_color_supplier_unittest.cc |  10 ++
 .../theme/page_theme_color_controller.cc      |  20 ++++
 .../theme/page_theme_color_controller.h       |   2 +
 chrome/browser/ui/stedding_ui_metrics.h       |   9 ++
 .../browser/ui/views/frame/app_menu_button.cc |  20 +++-
 .../ui/views/frame/browser_frame_view_win.cc  |  29 ++++-
 chrome/browser/ui/views/frame/browser_view.cc |  17 ++-
 .../ui/views/frame/browser_view_unittest.cc   |  12 +-
 .../layout/stedding_browser_view_layout.cc    |  67 +++++++++++-
 .../views/frame/stedding_window_background.cc |  13 ++-
 .../frame/vertical_tab_strip_region_view.cc   | 103 ++++++++++++++++--
 .../frame/vertical_tab_strip_region_view.h    |  11 ++
 .../ui/views/spaces/space_switcher_view.cc    |  37 ++++++-
 .../ui/views/spaces/space_switcher_view.h     |   7 ++
 .../tabs/common/dragged_tabs_container.cc     |  12 +-
 .../tabs/common/pinned_tab_container_view.cc  |  14 ++-
 .../tabs/common/tab_group_view_layout.cc      |  21 +++-
 .../ui/views/tabs/common/tab_strip_view.cc    |  22 ++++
 .../ui/views/tabs/common/tab_strip_view.h     |   4 +
 .../tabs/common/tab_strip_view_layout.cc      |  10 +-
 .../browser/ui/views/tabs/common/tab_view.cc  |   8 ++
 .../tabs/common/tab_view_vertical_layout.cc   |  15 ++-
 .../unpinned_tab_container_view_layout.cc     |  15 ++-
 .../vertical_tab_strip_top_container.cc       |  13 ++-
 .../browser/ui/views/toolbar/toolbar_view.cc  |  24 +++-
 .../browser/ui/views/toolbar/toolbar_view.h   |   6 +
 38 files changed, 574 insertions(+), 72 deletions(-)

diff --git a/chrome/browser/extensions/external_provider_impl.cc b/chrome/browser/extensions/external_provider_impl.cc
index de142296b3133..3cdba4296b7c9 100644
--- a/chrome/browser/extensions/external_provider_impl.cc
+++ b/chrome/browser/extensions/external_provider_impl.cc
@@ -863,12 +863,15 @@ void ExternalProviderImpl::CreateExternalProviders(
         bundled_extension_creation_flags));
 #endif
 #if BUILDFLAG(IS_WIN)
-    auto registry_provider = std::make_unique<ExternalProviderImpl>(
-        service, new ExternalRegistryLoader, profile,
-        ManifestLocation::kExternalRegistry,
-        ManifestLocation::kExternalPrefDownload, Extension::NO_FLAGS);
-    registry_provider->set_allow_updates(true);
-    provider_list->push_back(std::move(registry_provider));
+    // Stedding (privacy Q9): Chromium's registry loader reads Google
+    // Chrome's own key, Software\Google\Chrome\Extensions, whatever the
+    // build's branding, and installs what third-party installers put there
+    // from Google's update server -- on the operator's PC, Adobe's Acrobat
+    // extension, with an "Action required" chip on the app menu until the
+    // user accepted it (2026-09-08). Chrome's key is not Stedding's: a
+    // vendor that registered for Chrome did not register for this browser.
+    // No provider. Policy-forced extensions and the user's own installs
+    // come through other providers and stay.
 #else
     provider_list->push_back(std::make_unique<ExternalProviderImpl>(
         service,
diff --git a/chrome/browser/ui/layout_constants.cc b/chrome/browser/ui/layout_constants.cc
index 63d57aba46285..9d1e0fe2b06d7 100644
--- a/chrome/browser/ui/layout_constants.cc
+++ b/chrome/browser/ui/layout_constants.cc
@@ -6,6 +6,7 @@
 
 #include "base/notreached.h"
 #include "build/build_config.h"
+#include "base/feature_list.h"
 #include "chrome/browser/ui/stedding_ui_metrics.h"
 #include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/ui_features.h"
@@ -167,7 +168,10 @@ int GetLayoutConstant(LayoutConstant constant) {
     case LayoutConstant::kVerticalTabStripComboButtonIconSize:
       return 18;
     case LayoutConstant::kVerticalTabStripButtonIconSize:
-      return 20;
+      // Stedding: a size up, in step with the rail's favicons and the
+      // switcher's chips (operator, 2026-09-08).
+      return base::FeatureList::IsEnabled(stedding::kArcStyleWindow) ? 22
+                                                                       : 20;
     case LayoutConstant::kVerticalTabStripTopButtonPadding:
       return 4;
     case LayoutConstant::kVerticalTabStripFlatEdgeButtonPadding:
@@ -218,6 +222,13 @@ gfx::Insets GetLayoutInsets(LayoutInset inset) {
     case BROWSER_APP_MENU_CHIP_PADDING:
       if (touch_ui) {
         return GetLayoutInsets(TOOLBAR_BUTTON);
+      } else if (base::FeatureList::IsEnabled(stedding::kArcStyleWindow)) {
+        // Stedding (toolbar T25): the app menu's labelled chip -- "Action
+        // required", "Update" -- is the toolbar buttons' height, the
+        // button inset above and below its label, not Chromium's 34 DIP
+        // in a row whose field is 25 (operator, 2026-09-08).
+        return gfx::Insets::TLBR(stedding::kToolbarButtonInset.Get(), 4,
+                                 stedding::kToolbarButtonInset.Get(), 6);
       } else {
         return gfx::Insets::TLBR(7, 4, 7, 6);
       }
diff --git a/chrome/browser/ui/spaces/arc_data_importer.cc b/chrome/browser/ui/spaces/arc_data_importer.cc
index 6b70efac8e785..a334514674cff 100644
--- a/chrome/browser/ui/spaces/arc_data_importer.cc
+++ b/chrome/browser/ui/spaces/arc_data_importer.cc
@@ -153,9 +153,9 @@ ArcDataPaths ArcDataPathsFor(const base::FilePath& user_data_dir) {
   if (user_data_dir.empty()) {
     return paths;
   }
-  const base::FilePath profile = user_data_dir.Append("Default");
-  paths.history = profile.Append("History");
-  paths.logins = profile.Append("Login Data");
+  const base::FilePath profile = user_data_dir.Append(FILE_PATH_LITERAL("Default"));
+  paths.history = profile.Append(FILE_PATH_LITERAL("History"));
+  paths.logins = profile.Append(FILE_PATH_LITERAL("Login Data"));
   return paths;
 }
 
@@ -164,10 +164,10 @@ ArcDataPaths DefaultArcDataPaths() {
   if (!base::PathService::Get(base::DIR_HOME, &home)) {
     return ArcDataPaths();
   }
-  return ArcDataPathsFor(home.Append("Library")
-                             .Append("Application Support")
-                             .Append("Arc")
-                             .Append("User Data"));
+  return ArcDataPathsFor(home.Append(FILE_PATH_LITERAL("Library"))
+                             .Append(FILE_PATH_LITERAL("Application Support"))
+                             .Append(FILE_PATH_LITERAL("Arc"))
+                             .Append(FILE_PATH_LITERAL("User Data")));
 }
 
 bool ArcDataPresent(const ArcDataPaths& paths) {
diff --git a/chrome/browser/ui/spaces/arc_sidebar_importer.cc b/chrome/browser/ui/spaces/arc_sidebar_importer.cc
index 9fd0c4f6f6256..8e2f802878353 100644
--- a/chrome/browser/ui/spaces/arc_sidebar_importer.cc
+++ b/chrome/browser/ui/spaces/arc_sidebar_importer.cc
@@ -613,10 +613,10 @@ base::FilePath ArcSidebarPath() {
   if (!base::PathService::Get(base::DIR_HOME, &home)) {
     return base::FilePath();
   }
-  return home.Append("Library")
-      .Append("Application Support")
-      .Append("Arc")
-      .Append("StorableSidebar.json");
+  return home.Append(FILE_PATH_LITERAL("Library"))
+      .Append(FILE_PATH_LITERAL("Application Support"))
+      .Append(FILE_PATH_LITERAL("Arc"))
+      .Append(FILE_PATH_LITERAL("StorableSidebar.json"));
 }
 
 std::optional<std::string> ReadArcSidebar(const base::FilePath& path) {
@@ -628,7 +628,7 @@ std::optional<std::string> ReadArcSidebar(const base::FilePath& path) {
   base::ScopedTempDir temp;
   base::FilePath copy;
   if (temp.CreateUniqueTempDir()) {
-    copy = temp.GetPath().Append("StorableSidebar.json");
+    copy = temp.GetPath().Append(FILE_PATH_LITERAL("StorableSidebar.json"));
     if (!base::CopyFile(path, copy)) {
       copy.clear();
     }
diff --git a/chrome/browser/ui/spaces/sidebar_backup.cc b/chrome/browser/ui/spaces/sidebar_backup.cc
index 0446ebbd1caac..876a4e2ce7e65 100644
--- a/chrome/browser/ui/spaces/sidebar_backup.cc
+++ b/chrome/browser/ui/spaces/sidebar_backup.cc
@@ -334,20 +334,20 @@ std::string PlanToJson(const ArcImportPlan& plan) {
 }
 
 base::FilePath BackupDirectory(const base::FilePath& profile_path) {
-  return profile_path.Append("Sidebar Backups");
+  return profile_path.Append(FILE_PATH_LITERAL("Sidebar Backups"));
 }
 
 base::FilePath BackupFileName(base::Time when) {
   base::Time::Exploded exploded;
   when.LocalExplode(&exploded);
-  return base::FilePath(base::StringPrintf(
+  return base::FilePath::FromUTF8Unsafe(base::StringPrintf(
       "sidebar-%04d%02d%02d-%02d%02d%02d.json", exploded.year, exploded.month,
       exploded.day_of_month, exploded.hour, exploded.minute, exploded.second));
 }
 
 std::optional<base::Time> TimeFromBackupFileName(const base::FilePath& path) {
   // sidebar-YYYYMMDD-HHMMSS
-  const std::string name = path.BaseName().RemoveExtension().value();
+  const std::string name = path.BaseName().RemoveExtension().AsUTF8Unsafe();
   constexpr std::string_view kPrefix = "sidebar-";
   constexpr size_t kDigits = 15;
   if (name.size() != kPrefix.size() + kDigits || !name.starts_with(kPrefix) ||
@@ -483,9 +483,13 @@ base::FilePath WriteExportOnPool(const base::FilePath& directory,
                                  const std::string& json) {
   base::Time::Exploded exploded;
   base::Time::Now().LocalExplode(&exploded);
-  const base::FilePath path = directory.Append(base::StringPrintf(
-      "%s %04d-%02d-%02d %02d.%02d.json", stem.c_str(), exploded.year,
-      exploded.month, exploded.day_of_month, exploded.hour, exploded.minute));
+  // The stem carries the Space's name, which is the user's text: UTF-8, not
+  // ASCII (AppendASCII DCHECKs on anything else, and mangles it on Windows).
+  const base::FilePath path =
+      directory.Append(base::FilePath::FromUTF8Unsafe(base::StringPrintf(
+          "%s %04d-%02d-%02d %02d.%02d.json", stem.c_str(), exploded.year,
+          exploded.month, exploded.day_of_month, exploded.hour,
+          exploded.minute)));
   return base::CreateDirectory(directory) && base::WriteFile(path, json)
              ? path
              : base::FilePath();
diff --git a/chrome/browser/ui/stedding/address_row.cc b/chrome/browser/ui/stedding/address_row.cc
index 9bcb9d7aed7db..43d5b4f4243f6 100644
--- a/chrome/browser/ui/stedding/address_row.cc
+++ b/chrome/browser/ui/stedding/address_row.cc
@@ -65,6 +65,7 @@ bool AddressRow::IsVisible() {
     }
   }
   signals.override = override_;
+  signals.caption_buttons_over_row = caption_buttons_over_row_;
   return ShouldShowAddressRow(signals);
 }
 
diff --git a/chrome/browser/ui/stedding/address_row.h b/chrome/browser/ui/stedding/address_row.h
index ed1eeaa985506..ce0d2c149dae2 100644
--- a/chrome/browser/ui/stedding/address_row.h
+++ b/chrome/browser/ui/stedding/address_row.h
@@ -41,6 +41,12 @@ class AddressRow : public TabStripModelObserver {
 
   std::optional<bool> override_for_testing() const { return override_; }
 
+  // T22: the window tells the row whether the frame's own buttons sit over
+  // its column (Windows' caption buttons); the rule keeps the row then.
+  void set_caption_buttons_over_row(bool over) {
+    caption_buttons_over_row_ = over;
+  }
+
   // TabStripModelObserver: the localhost exception follows the active tab.
   void OnTabStripModelChanged(
       TabStripModel* tab_strip_model,
@@ -54,6 +60,7 @@ class AddressRow : public TabStripModelObserver {
   const raw_ref<BrowserWindowInterface> browser_;
   std::optional<bool> override_;
   std::optional<bool> last_collapsed_;
+  bool caption_buttons_over_row_ = false;
   PrefChangeRegistrar pref_registrar_;
   std::optional<ui::ScopedUnownedUserData<AddressRow>> scoped_user_data_;
 };
diff --git a/chrome/browser/ui/stedding/address_row_rules.cc b/chrome/browser/ui/stedding/address_row_rules.cc
index d45cfa2298553..22dcf211eb64e 100644
--- a/chrome/browser/ui/stedding/address_row_rules.cc
+++ b/chrome/browser/ui/stedding/address_row_rules.cc
@@ -13,7 +13,8 @@ bool ShouldShowAddressRow(const AddressRowSignals& signals) {
   if (signals.override.has_value()) {
     return *signals.override;
   }
-  if (!signals.hide_with_sidebar || signals.localhost) {
+  if (!signals.hide_with_sidebar || signals.localhost ||
+      signals.caption_buttons_over_row) {
     return true;
   }
   return !signals.sidebar_collapsed;
diff --git a/chrome/browser/ui/stedding/address_row_rules.h b/chrome/browser/ui/stedding/address_row_rules.h
index 565a53ab1a07a..8028f84133fab 100644
--- a/chrome/browser/ui/stedding/address_row_rules.h
+++ b/chrome/browser/ui/stedding/address_row_rules.h
@@ -22,6 +22,10 @@ struct AddressRowSignals {
   bool localhost = false;
   // shift-cmd-D's answer for the window, until the sidebar changes (T10).
   std::optional<bool> override;
+  // The frame's own buttons sit over the row's column -- Windows' caption
+  // buttons, the frame's trailing exclusion -- so a top strip exists
+  // whatever the sidebar does (T22).
+  bool caption_buttons_over_row = false;
 };
 
 bool ShouldShowAddressRow(const AddressRowSignals& signals);
diff --git a/chrome/browser/ui/stedding/address_row_rules_unittest.cc b/chrome/browser/ui/stedding/address_row_rules_unittest.cc
index 2d5f6299c788f..7faf328a1c522 100644
--- a/chrome/browser/ui/stedding/address_row_rules_unittest.cc
+++ b/chrome/browser/ui/stedding/address_row_rules_unittest.cc
@@ -33,5 +33,18 @@ TEST(AddressRowRulesTest, HiddenWithTheSidebarUnlessLocalhostOrOverridden) {
   EXPECT_FALSE(IsLocalhostPage(GURL("chrome://settings/")));
 }
 
+// T22: with the frame's own buttons over the row's column -- Windows --
+// a top strip exists whatever the sidebar does, and the row keeps it;
+// the toggle still wins.
+TEST(AddressRowRulesTest, KeptUnderTheCaptionButtons) {
+  AddressRowSignals signals;
+  signals.sidebar_collapsed = true;
+  EXPECT_FALSE(ShouldShowAddressRow(signals)) << "T8: collapsed hides it";
+  signals.caption_buttons_over_row = true;
+  EXPECT_TRUE(ShouldShowAddressRow(signals)) << "T22: the strip is there";
+  signals.override = false;
+  EXPECT_FALSE(ShouldShowAddressRow(signals)) << "T10: the toggle wins";
+}
+
 }  // namespace
 }  // namespace stedding
diff --git a/chrome/browser/ui/stedding/screenshot/screenshot_capture.cc b/chrome/browser/ui/stedding/screenshot/screenshot_capture.cc
index feb26e3f27216..bd99261e23a30 100644
--- a/chrome/browser/ui/stedding/screenshot/screenshot_capture.cc
+++ b/chrome/browser/ui/stedding/screenshot/screenshot_capture.cc
@@ -63,7 +63,7 @@ base::FilePath FileNameFor(content::WebContents* contents) {
   }
   const std::string stamp = base::UnlocalizedTimeFormatWithPattern(
       base::Time::Now(), "yyyy-MM-dd HH.mm.ss");
-  return base::FilePath("Stedding " + host + " " + stamp + ".png");
+  return base::FilePath::FromUTF8Unsafe("Stedding " + host + " " + stamp + ".png");
 }
 
 base::FilePath& LastCapturePathStorage() {
diff --git a/chrome/browser/ui/stedding/theme/page_bar_color_supplier.cc b/chrome/browser/ui/stedding/theme/page_bar_color_supplier.cc
index b09bd87aadbc5..e9ff41ae6f4bd 100644
--- a/chrome/browser/ui/stedding/theme/page_bar_color_supplier.cc
+++ b/chrome/browser/ui/stedding/theme/page_bar_color_supplier.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/ui/stedding/theme/page_bar_color_supplier.h"
 
+#include "build/build_config.h"
+
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "ui/color/color_mixer.h"
 #include "ui/color/color_provider.h"
@@ -43,8 +45,33 @@ void PageBarColorSupplier::AddColorMixers(
   mixer[kColorLocationBarBackground] = {SK_ColorTRANSPARENT};
   mixer[kColorLocationBarBackgroundHovered] = ui::SetAlpha(fg, 0x14);
   mixer[kColorOmniboxIconBackground] = ui::SetAlpha(fg, 0x1A);
+  // Focused, the location bar borrows the results dropdown's background
+  // (LocationBarView::GetBackgroundColor, while the caret shows), and that is
+  // the theme's own -- near-black in dark mode, on a white page's row
+  // (operator, 2026-09-08). Both follow the page instead, as Safari's do; the
+  // dropdown's text, icons and selection keep the bar's contrast colour so
+  // they stay readable on it whichever side of the line the page is on.
+  mixer[kColorOmniboxResultsBackground] = bar;
+  mixer[kColorOmniboxResultsBackgroundIph] = bar;
+  mixer[kColorOmniboxResultsBackgroundHovered] = ui::AlphaBlend(fg, bar, 0x14);
+  mixer[kColorOmniboxResultsBackgroundSelected] =
+      ui::AlphaBlend(fg, bar, 0x24);
+  mixer[kColorOmniboxResultsTextSelected] = fg;
+  mixer[kColorOmniboxResultsTextDimmed] = ui::SetAlpha(fg, 0xB3);
+  mixer[kColorOmniboxResultsTextDimmedSelected] = ui::SetAlpha(fg, 0xB3);
+  mixer[kColorOmniboxResultsUrl] = ui::SetAlpha(fg, 0xCC);
+  mixer[kColorOmniboxResultsIcon] = fg;
+  mixer[kColorOmniboxResultsIconSelected] = fg;
+  mixer[kColorOmniboxResultsIconHovered] = fg;
   // No hairline between the bar and the card: the separator is the bar.
   mixer[kColorToolbarContentAreaSeparator] = bar;
+#if BUILDFLAG(IS_WIN)
+  // Windows' caption buttons sit on the bar (toolbar T19): with no
+  // horizontal tab strip WindowsCaptionButton reads this one, and the
+  // theme's white vanished on a white page's row. The id exists on
+  // Windows alone (chrome_color_id.h), so the row is guarded.
+  mixer[kColorCaptionButtonOnToolbar] = fg;
+#endif
 }
 
 }  // namespace stedding
diff --git a/chrome/browser/ui/stedding/theme/page_bar_color_supplier_unittest.cc b/chrome/browser/ui/stedding/theme/page_bar_color_supplier_unittest.cc
index 9d224d618b4af..320991c1592e4 100644
--- a/chrome/browser/ui/stedding/theme/page_bar_color_supplier_unittest.cc
+++ b/chrome/browser/ui/stedding/theme/page_bar_color_supplier_unittest.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/ui/stedding/theme/page_bar_color_supplier.h"
 
+#include "build/build_config.h"
+
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "testing/gtest/include/gtest/gtest.h"
 #include "third_party/skia/include/core/SkColor.h"
@@ -32,6 +34,10 @@ TEST(PageBarColorSupplierTest, IconsContrastWithThePageColour) {
       << "the star follows the toolbar's icons";
   EXPECT_EQ(on_white.GetColor(kColorToolbarButtonIcon),
             on_white.GetColor(kColorOmniboxText));
+#if BUILDFLAG(IS_WIN)
+  EXPECT_TRUE(color_utils::IsDark(on_white.GetColor(kColorCaptionButtonOnToolbar)))
+      << "T19: Windows' caption glyphs sit on the bar";
+#endif
 
   ui::ColorProvider on_graphite;
   PageBarColorSupplier graphite(SkColorSetRGB(0x1F, 0x24, 0x30));
@@ -39,6 +45,10 @@ TEST(PageBarColorSupplierTest, IconsContrastWithThePageColour) {
   on_graphite.GenerateColorMapForTesting();
   EXPECT_FALSE(
       color_utils::IsDark(on_graphite.GetColor(kColorOmniboxActionIcon)));
+#if BUILDFLAG(IS_WIN)
+  EXPECT_FALSE(color_utils::IsDark(
+      on_graphite.GetColor(kColorCaptionButtonOnToolbar)));
+#endif
 }
 
 }  // namespace
diff --git a/chrome/browser/ui/stedding/theme/page_theme_color_controller.cc b/chrome/browser/ui/stedding/theme/page_theme_color_controller.cc
index 6e4162c8b9b9c..1627843bf6a58 100644
--- a/chrome/browser/ui/stedding/theme/page_theme_color_controller.cc
+++ b/chrome/browser/ui/stedding/theme/page_theme_color_controller.cc
@@ -83,6 +83,16 @@ void PageThemeColorController::PrimaryPageChanged(content::Page& page) {
   Refresh();
 }
 
+void PageThemeColorController::DidFirstVisuallyNonEmptyPaint() {
+  // The Page's colour is populated by now whether or not a change was ever
+  // announced; see Refresh.
+  Refresh();
+}
+
+void PageThemeColorController::DidStopLoading() {
+  Refresh();
+}
+
 void PageThemeColorController::WebContentsDestroyed() {
   Observe(nullptr);
   Refresh();
@@ -102,6 +112,16 @@ void PageThemeColorController::Refresh() {
       // The bar is an opaque surface.
       color = SkColorSetA(*page, SK_AlphaOPAQUE);
     }
+    // A reload: the new Page starts with no colour of its own, and it reports
+    // one only when it differs from the last colour the WebContents sent
+    // (WebContentsImpl::OnBackgroundColorChanged keeps that on the
+    // WebContents, not the Page), so a white page reloading to white is never
+    // announced and the row dropped to the window ground and stayed there
+    // (operator, 2026-09-08). Keep the colour while the page loads; the first
+    // paint and the end of loading re-read the Page, which has it by then.
+    if (!color && color_ && contents->IsLoading()) {
+      return;
+    }
   }
   SetColor(color);
 }
diff --git a/chrome/browser/ui/stedding/theme/page_theme_color_controller.h b/chrome/browser/ui/stedding/theme/page_theme_color_controller.h
index ca534ab97c48e..5623e2635275a 100644
--- a/chrome/browser/ui/stedding/theme/page_theme_color_controller.h
+++ b/chrome/browser/ui/stedding/theme/page_theme_color_controller.h
@@ -65,6 +65,8 @@ class PageThemeColorController : public TabStripModelObserver,
   void DidChangeThemeColor() override;
   void OnBackgroundColorChanged() override;
   void PrimaryPageChanged(content::Page& page) override;
+  void DidFirstVisuallyNonEmptyPaint() override;
+  void DidStopLoading() override;
   void WebContentsDestroyed() override;
 
   // The change path without a page, for tests.
diff --git a/chrome/browser/ui/stedding_ui_metrics.h b/chrome/browser/ui/stedding_ui_metrics.h
index f45849c3a7143..3d446fa4d3091 100644
--- a/chrome/browser/ui/stedding_ui_metrics.h
+++ b/chrome/browser/ui/stedding_ui_metrics.h
@@ -62,6 +62,15 @@ int SidebarPinnedHeight();
 int SidebarFaviconSize();
 int SidebarCornerRadius();
 
+// sidebar Y8: a row's side padding inside the strip while the sidebar is
+// the rail. The strip is the rail less the card gutter's worth of interior
+// margin (VerticalTabStripRegionView::UpdateInteriorMargin), so a rail row
+// is kCollapsedWidth - kCardGutter - 2 * kRailRowPadding wide: 44 DIP, a
+// square a row tall, centred in the rail plus the gutter (Y6). The open
+// sidebar's padding is upstream's kVerticalTabStripHorizontalPadding, 12,
+// which left a 26 DIP pill in a 62 DIP column (operator, 2026-09-08).
+inline constexpr int kRailRowPadding = 3;
+
 // Height of one tab row in the vertical tab strip, in DIPs.
 BASE_DECLARE_FEATURE_PARAM(int, kVerticalTabHeight);
 
diff --git a/chrome/browser/ui/views/frame/app_menu_button.cc b/chrome/browser/ui/views/frame/app_menu_button.cc
index e9c60785c25d0..f8e08ac7a1c9d 100644
--- a/chrome/browser/ui/views/frame/app_menu_button.cc
+++ b/chrome/browser/ui/views/frame/app_menu_button.cc
@@ -19,6 +19,8 @@
 #include "chrome/browser/ui/views/app_menu/action_app_menu.h"
 #include "chrome/browser/ui/views/chrome_typography.h"
 #include "chrome/browser/ui/views/frame/app_menu_button_observer.h"
+#include "chrome/browser/ui/views/frame/browser_view.h"
+#include "chrome/browser/ui/views/frame/top_container_view.h"
 #include "chrome/browser/ui/views/interaction/browser_elements_views.h"
 #include "chrome/browser/ui/views/toolbar/app_menu.h"
 #include "chrome/browser/ui/views/toolbar/toolbar_ink_drop_util.h"
@@ -57,7 +59,23 @@ views::BubbleAnchor AppMenuButton::GetAnchor() {
   auto* const elements = BrowserElementsViews::From(this);
   CHECK(elements);
   auto* const element = elements->GetElement(kFallbackPopupAnchorElementId);
-  CHECK(element);
+  // Stedding: the fallback anchor is a tracked element, and the tracker
+  // knows a view only once its widget is visible. A bubble asked for before
+  // the window is shown -- the crashed-session "Restore pages?" bubble at
+  // startup -- while the address row is hidden (toolbar T8: this button
+  // not drawn) found none, and the CHECK here killed every launch of that
+  // profile (HANDOFF trap 35, 2026-09-08). The top container is laid out
+  // from the start and is where the button would be.
+  if (!element) {
+    if (views::Widget* const widget = GetWidget()) {
+      if (BrowserView* const browser_view =
+              BrowserView::GetBrowserViewForNativeWindow(
+                  widget->GetNativeWindow())) {
+        return views::BubbleAnchor(browser_view->top_container());
+      }
+    }
+    return views::BubbleAnchor(this);
+  }
   return views::BubbleAnchor(element);
 }
 
diff --git a/chrome/browser/ui/views/frame/browser_frame_view_win.cc b/chrome/browser/ui/views/frame/browser_frame_view_win.cc
index 207ea26540d4d..07c13a42d3181 100644
--- a/chrome/browser/ui/views/frame/browser_frame_view_win.cc
+++ b/chrome/browser/ui/views/frame/browser_frame_view_win.cc
@@ -23,6 +23,8 @@
 #include "chrome/browser/ui/ui_features.h"
 #include "chrome/browser/ui/view_ids.h"
 #include "chrome/browser/ui/views/frame/browser_caption_button_container_win.h"
+#include "base/feature_list.h"
+#include "chrome/browser/ui/stedding_ui_metrics.h"
 #include "chrome/browser/ui/views/frame/browser_view.h"
 #include "chrome/browser/ui/views/frame/minimize_button_metrics_win.h"
 #include "chrome/browser/ui/views/frame/tab_strip_region_view.h"
@@ -737,14 +739,31 @@ void BrowserFrameViewWin::LayoutCaptionButtons() {
   const gfx::Size preferred_size =
       caption_button_container_->GetPreferredSize();
 
-  const int height =
+  int height =
       !GetBrowserView()->GetWebAppFrameToolbarPreferredSize().IsEmpty()
           ? (TitlebarHeight(false) - WindowTopY())
           : GetFrameHeight();
-
-  caption_button_container_->SetBounds(width() - preferred_size.width(),
-                                       WindowTopY(), preferred_size.width(),
-                                       height);
+  int y = WindowTopY();
+  // Stedding (toolbar T19): with the tab strip vertical the buttons sit on
+  // the address row, so they centre on it -- their own height, on the
+  // row's centre line -- instead of stretching down from the window's top
+  // edge, which put their centre 4 DIP above the row's (operator,
+  // 2026-09-08). The row is the card gutter below the client's top and
+  // the toolbar's preferred height.
+  const ToolbarView* const toolbar = GetBrowserView()->toolbar();
+  if (base::FeatureList::IsEnabled(stedding::kArcStyleWindow) && toolbar &&
+      GetClientFrameElementInfo().tabstrip_preferred_height == 0 &&
+      GetBrowserView()->IsToolbarVisible() &&
+      GetBrowserView()->GetWebAppFrameToolbarPreferredSize().IsEmpty() &&
+      !browser_widget()->IsFullscreen()) {
+    const int row_top = GetTopInset(false) + stedding::kCardGutter.Get();
+    const int row_height = toolbar->GetPreferredSize().height();
+    height = preferred_size.height();
+    y = row_top + std::max(0, (row_height - height) / 2);
+  }
+
+  caption_button_container_->SetBounds(width() - preferred_size.width(), y,
+                                       preferred_size.width(), height);
 }
 
 void BrowserFrameViewWin::LayoutClientView() {
diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index e175007c552b7..89e0c15699559 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -3049,10 +3049,19 @@ void BrowserView::DisableTabStripEditingForTesting() {
 
 bool BrowserView::IsToolbarVisible() const {
   // Stedding (docs/features/toolbar.md T8, T10): the address row hides with
-  // the sidebar, or by the window's own toggle.
-  if (stedding::AddressRow* const row = stedding::AddressRow::From(browser_);
-      row && !row->IsVisible()) {
-    return false;
+  // the sidebar, or by the window's own toggle -- unless the frame's own
+  // buttons sit over its column (T22: Windows' caption buttons, the
+  // trailing exclusion), when the strip exists whatever the sidebar does
+  // and the row keeps it; hidden, it left an empty band and no address bar
+  // (operator, 2026-09-08).
+  if (stedding::AddressRow* const row = stedding::AddressRow::From(browser_)) {
+    if (const BrowserFrameView* const frame_view = GetFrameView()) {
+      row->set_caption_buttons_over_row(
+          !frame_view->GetBrowserLayoutParams().trailing_exclusion.IsEmpty());
+    }
+    if (!row->IsVisible()) {
+      return false;
+    }
   }
 #if BUILDFLAG(IS_MAC)
   // Immersive full screen makes it possible to display the toolbar when
diff --git a/chrome/browser/ui/views/frame/browser_view_unittest.cc b/chrome/browser/ui/views/frame/browser_view_unittest.cc
index 3377f1319348f..41d1c5d7815b1 100644
--- a/chrome/browser/ui/views/frame/browser_view_unittest.cc
+++ b/chrome/browser/ui/views/frame/browser_view_unittest.cc
@@ -538,8 +538,16 @@ TEST_F(BrowserViewTest, WindowTitleOmitsLowMemoryUsage) {
   memory_usage.tab_resource_usage = tab_resource_usage_;
 
   AddTab(browser(), GURL("about:blank"));
-  Tab* const tab =
-      browser_view()->horizontal_tab_strip_for_testing()->tab_at(0);
+  // Stedding (ADR 0010): the sidebar's vertical strip is the tab strip here,
+  // so the horizontal strip this case reads has no tab to hand out and the
+  // test dereferenced null (the Mac pass, 2026-09-10). The title's memory
+  // note is upstream's; the horizontal strip is not a Stedding surface.
+  TabStrip* const horizontal =
+      browser_view()->horizontal_tab_strip_for_testing();
+  if (!horizontal || horizontal->GetTabCount() == 0) {
+    GTEST_SKIP() << "no horizontal tab strip in an Arc window (ADR 0010)";
+  }
+  Tab* const tab = horizontal->tab_at(0);
   tab->SetDataForTesting(std::move(memory_usage));
 
   // Expect that low memory usage isn't in the window title.
diff --git a/chrome/browser/ui/views/frame/layout/stedding_browser_view_layout.cc b/chrome/browser/ui/views/frame/layout/stedding_browser_view_layout.cc
index 57b53b73df39e..1692774315a77 100644
--- a/chrome/browser/ui/views/frame/layout/stedding_browser_view_layout.cc
+++ b/chrome/browser/ui/views/frame/layout/stedding_browser_view_layout.cc
@@ -18,6 +18,26 @@
 #include "chrome/browser/ui/views/toolbar/toolbar_view.h"
 #include "ui/gfx/geometry/insets.h"
 #include "ui/gfx/geometry/rounded_corners_f.h"
+#include "ui/views/view_utils.h"
+
+namespace {
+
+// toolbar T19: how much of the row the frame's trailing exclusion --
+// Windows' caption buttons, measured from the window's edge -- takes past
+// the card gutter the row keeps there anyway. Zero without a vertical strip
+// or without such buttons (macOS: the traffic lights are the leading
+// exclusion, over the sidebar's column).
+int TrailingReserve(const BrowserLayoutParams& params,
+                    bool vertical_strip) {
+  if (!vertical_strip || params.trailing_exclusion.IsEmpty()) {
+    return 0;
+  }
+  return std::max(
+      0, base::ClampCeil(params.trailing_exclusion.ContentWithPadding().width()) -
+             stedding::kCardGutter.Get());
+}
+
+}  // namespace
 
 SteddingBrowserViewLayout::SteddingBrowserViewLayout(
     std::unique_ptr<BrowserViewLayoutDelegate> delegate,
@@ -79,9 +99,15 @@ SteddingBrowserViewLayout::CalculateProposedLayout(
     // DIP circles. Half the exclusion's height sat the toggle 3 DIP low
     // against a capture of the real window (2026-09-05).
     constexpr int kTrafficLightGlyph = 12;
+    // Windows has no leading exclusion: its caption buttons are at the top
+    // right (trailing_exclusion, handled in CalculateTopContainerLayout), so
+    // there are no lights to centre on and the toggle takes the container's
+    // own baseline instead of a centre computed from an empty box.
     strip->SetCaptionButtonCenterForLayout(
-        base::ClampCeil(params.leading_exclusion.vertical_padding) +
-        kTrafficLightGlyph / 2);
+        params.leading_exclusion.IsEmpty()
+            ? 0
+            : base::ClampCeil(params.leading_exclusion.vertical_padding) +
+                  kTrafficLightGlyph / 2);
   }
   return layout;
 }
@@ -122,6 +148,18 @@ gfx::Rect SteddingBrowserViewLayout::CalculateTopContainerLayout(
   const int kCardSideInset = stedding::kCardGutter.Get();
   params.visual_client_area.Inset(
       gfx::Insets::TLBR(kCardSideInset, kCardSideInset, 0, kCardSideInset));
+  // The exclusion above is skipped with a vertical strip because macOS's
+  // traffic lights sit over the sidebar's column. Windows' caption buttons
+  // are frame-owned controls at the top RIGHT, over this row's column, and
+  // the row ran under them (operator, 2026-09-08). The frame reports them as
+  // the trailing exclusion, measured from the window's edge; the row's
+  // contents give up that much (the bar itself runs on under the buttons:
+  // SteddingWindowBackground::PaintPageBar).
+  if (const int trailing = TrailingReserve(
+          params, delegate().ShouldDrawVerticalTabStrip());
+      trailing > 0) {
+    params.visual_client_area.Inset(gfx::Insets::TLBR(0, 0, 0, trailing));
+  }
   gfx::Rect result = BrowserViewTabbedLayoutImpl::CalculateTopContainerLayout(
       layout, std::move(params),
       needs_exclusion && !delegate().ShouldDrawVerticalTabStrip());
@@ -133,6 +171,17 @@ gfx::Rect SteddingBrowserViewLayout::CalculateTopContainerLayout(
   // clipped, 2026-09-05).
   result.set_y(result.y() - kCardSideInset);
   result.set_height(result.height() + kCardSideInset);
+  // With the address row hidden (toolbar T8) this container is empty and the
+  // card rose to the window's top -- under Windows' caption buttons, which
+  // could then not be reached at all from the rail (operator, 2026-09-08).
+  // The container keeps the caption strip's height, so the card starts
+  // below the buttons, on the mat the frame draws them over.
+  if (!delegate().IsToolbarVisible() && delegate().ShouldDrawVerticalTabStrip() &&
+      !params.trailing_exclusion.IsEmpty()) {
+    result.set_height(std::max(
+        result.height(),
+        base::ClampCeil(params.trailing_exclusion.ContentWithPadding().height())));
+  }
   // The children were placed past the strip inside the container; the
   // container itself still spans the overlay, or the offset would apply
   // twice.
@@ -179,6 +228,17 @@ void SteddingBrowserViewLayout::DoPostLayoutVisualAdjustments(
       toolbar_background->SetVisible(false);
     }
   }
+  // toolbar T19: the row's contents stop short of Windows' caption buttons
+  // but its bar runs on under them, so the address field centres on the
+  // bar (toolbar T18), not on the contents' width.
+  if (ToolbarView* const toolbar =
+          views().toolbar ? views::AsViewClass<ToolbarView>(views().toolbar)
+                          : nullptr) {
+    toolbar->SetSteddingTrailingReserve(
+        delegate().IsToolbarVisible()
+            ? TrailingReserve(params, delegate().ShouldDrawVerticalTabStrip())
+            : 0);
+  }
 
   MultiContentsView* const contents = views().multi_contents_view;
   if (!contents) {
@@ -197,6 +257,9 @@ void SteddingBrowserViewLayout::DoPostLayoutVisualAdjustments(
   // showed the row's colour through its rounded top corners until
   // 2026-09-05. With the row hidden (T8) the page is the whole card.
   const float ours = static_cast<float>(stedding::kContentsCornerRadius.Get());
+  // Under Windows' caption buttons too: the bar runs on to the card's edge
+  // there (toolbar T19), so the card's top-right corner is under it like
+  // the left one.
   const float top = delegate().IsToolbarVisible() ? 0.f : ours;
   const gfx::RoundedCornersF current = contents->GetBackgroundRadii();
   const gfx::RoundedCornersF merged(top, top,
diff --git a/chrome/browser/ui/views/frame/stedding_window_background.cc b/chrome/browser/ui/views/frame/stedding_window_background.cc
index daea5715b64eb..6dc3c699fd642 100644
--- a/chrome/browser/ui/views/frame/stedding_window_background.cc
+++ b/chrome/browser/ui/views/frame/stedding_window_background.cc
@@ -168,8 +168,17 @@ void SteddingWindowBackground::PaintPageBar(gfx::Canvas* canvas,
   const float radius =
       static_cast<float>(stedding::kContentsCornerRadius.Get());
   // From the toolbar's top to the card's top edge: the page starts square
-  // under the row (toolbar T16), so nothing runs under it.
-  gfx::RectF surface(toolbar.x(), toolbar.y(), toolbar.width(),
+  // under the row (toolbar T16), so nothing runs under it. And to the
+  // card's right edge, not the toolbar's: on Windows the toolbar's contents
+  // stop short of the frame's caption buttons (toolbar T19), but the bar
+  // runs on under them, the buttons painting their glyphs in its contrast
+  // colour (PageBarColorSupplier). Stopping with the contents left the
+  // row's end as a cut corner beside the buttons (operator, 2026-09-08).
+  // On macOS the two edges coincide.
+  const float bar_right =
+      card.right() - static_cast<float>(stedding::kCardGutter.Get());
+  gfx::RectF surface(toolbar.x(), toolbar.y(),
+                     std::max(toolbar.width(), bar_right - toolbar.x()),
                      card.y() - toolbar.y());
   cc::PaintFlags flags;
   flags.setAntiAlias(true);
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
index 3798e8a29b2eb..5f2409d2876f3 100644
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.cc
@@ -247,9 +247,11 @@ VerticalTabStripRegionView::VerticalTabStripRegionView(
     archived_row_ = AddChildView(std::move(archived_row));
 
     auto* const bottom_row = AddChildView(std::make_unique<views::View>());
+    bottom_row_ = bottom_row;
     auto* const row_layout =
         bottom_row->SetLayoutManager(std::make_unique<views::BoxLayout>(
             views::BoxLayout::Orientation::kHorizontal));
+    bottom_row_layout_ = row_layout;
     row_layout->set_cross_axis_alignment(
         views::BoxLayout::CrossAxisAlignment::kCenter);
     if (bottom_button_container_) {
@@ -491,21 +493,14 @@ void VerticalTabStripRegionView::UpdateInteriorMargin(bool collapsed) {
       collapsed && base::FeatureList::IsEnabled(stedding::kArcStyleWindow)
           ? stedding::kCardGutter.Get()
           : 0;
-  const int padding =
-      GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
   flex_layout_->SetInteriorMargin(gfx::Insets::TLBR(
       0, left,
       GetLayoutConstant(
           LayoutConstant::kVerticalTabStripUncollapsedVerticalPadding),
       0));
-  top_button_container_->SetProperty(
-      views::kMarginsKey, gfx::Insets::TLBR(0, padding + left, 0, padding));
-  bottom_button_container_->SetProperty(
-      views::kMarginsKey,
-      gfx::Insets::TLBR(
-          GetLayoutConstant(
-              LayoutConstant::kVerticalTabStripCollapsedVerticalPadding),
-          padding + left, 0, padding));
+  // The button containers' side margins are the rail form's (sidebar Y8).
+  rail_left_ = left;
+  ApplyRailForm(collapsed);
 }
 
 void VerticalTabStripRegionView::SetCaptionButtonCenterForLayout(
@@ -665,6 +660,20 @@ void VerticalTabStripRegionView::RemovedFromWidget() {
 void VerticalTabStripRegionView::Layout(PassKey) {
   LayoutSuperclass<views::AccessiblePaneView>(this);
 
+  // Stedding: the strip's own background is invisible so the window's
+  // gradient shows through (HANDOFF trap 8). Right while the strip sits
+  // beside the page; wrong while expand-on-hover floats it OVER the page as a
+  // layer that does not fill its bounds opaquely, where every row read
+  // through to the page under it (operator, 2026-09-08). Opaque exactly then:
+  // collapsed by state, drawn wider than the rail.
+  if (base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+      background()) {
+    if (auto* const ground = background()->AsA<CustomCornersBackground>()) {
+      ground->SetVisible(state_controller_->IsCollapsed() &&
+                         width() > kCollapsedWidth);
+    }
+  }
+
   // Manually position the resize area as it overlaps views handled by the flex
   // layout.
   resize_area_->SetBoundsRect(gfx::Rect(bounds().right() - resize_area_width_,
@@ -1023,6 +1032,9 @@ void VerticalTabStripRegionView::OnTabStripViewSet() {
               LayoutConstant::kVerticalTabStripCollapsedVerticalPadding),
           0));
 
+  // The rail form reaches the strip's own rows from now on (sidebar Y8).
+  ApplyRailForm(state_controller_->IsCollapsed() && !is_expanded_on_hover_);
+
   // Pre-set the animation values to the appropriate state.
   auto* const animation_controller =
       BrowserAnimationController::From(browser_view()->browser());
@@ -1461,9 +1473,80 @@ void VerticalTabStripRegionView::ResetExpandOnHoverTimers() {
   }
 }
 
+void VerticalTabStripRegionView::ApplyRailForm(bool rail) {
+  const bool arc_style =
+      base::FeatureList::IsEnabled(stedding::kArcStyleWindow);
+  // The button containers sit on the rows' column: the rail's padding
+  // from the strip's edges in the rail, the open sidebar's 12 DIP
+  // otherwise, past the gutter's worth of interior margin the collapsed
+  // strip carries (sidebar Y6, Y8). With the open padding the toggle sat
+  // 3.5 DIP right of the rows' centre in a container narrower than itself.
+  const int padding =
+      arc_style && rail
+          ? stedding::kRailRowPadding
+          : GetLayoutConstant(
+                LayoutConstant::kVerticalTabStripHorizontalPadding);
+  top_button_container_->SetProperty(
+      views::kMarginsKey,
+      gfx::Insets::TLBR(0, padding + rail_left_, 0, padding));
+  bottom_button_container_->SetProperty(
+      views::kMarginsKey,
+      gfx::Insets::TLBR(
+          GetLayoutConstant(
+              LayoutConstant::kVerticalTabStripCollapsedVerticalPadding),
+          padding + rail_left_, 0, padding));
+  if (!arc_style) {
+    return;
+  }
+  // The strip's own "+ New Tab" row takes the same form.
+  if (tab_strip_view()) {
+    tab_strip_view()->SetRailForm(rail);
+  }
+  // The Archived row is its icon alone in the rail, centred like the tab rows'
+  // favicons and a size up like them; the label and its 18 DIP inset are for
+  // the open sidebar.
+  if (archived_row_) {
+    auto* const row = static_cast<SidebarRowButton*>(archived_row_.get());
+    row->SetHorizontalAlignment(rail ? gfx::ALIGN_CENTER : gfx::ALIGN_LEFT);
+    row->SetText(rail ? std::u16string() : u"Archived");
+    row->SetBorder(views::CreateEmptyBorder(gfx::Insets::VH(0, rail ? 0 : 18)));
+    row->SetImageModel(
+        views::Button::STATE_NORMAL,
+        ui::ImageModel::FromVectorIcon(
+            vector_icons::kHistoryIcon, kColorTabForegroundInactiveFrameActive,
+            stedding::SidebarFaviconSize() + (rail ? 4 : 0)));
+  }
+  if (space_switcher_) {
+    space_switcher_->SetCollapsed(rail);
+  }
+  // The downloads button and the switcher stack in the rail instead of
+  // sharing a row the rail is too narrow for (the plus ended up over the page).
+  if (bottom_row_layout_) {
+    bottom_row_layout_->SetOrientation(
+        rail ? views::BoxLayout::Orientation::kVertical
+             : views::BoxLayout::Orientation::kHorizontal);
+  }
+  if (downloads_button_) {
+    downloads_button_->SetProperty(
+        views::kMarginsKey,
+        rail ? gfx::Insets::TLBR(0, 0, 8, 0) : gfx::Insets::TLBR(0, 10, 0, 0));
+  }
+  if (bottom_row_ && space_switcher_) {
+    int height = space_switcher_->GetPreferredSize().height();
+    if (rail && downloads_button_) {
+      height += downloads_button_->GetPreferredSize().height() + 8;
+    }
+    bottom_row_->SetPreferredSize(gfx::Size(0, height));
+  }
+}
+
 void VerticalTabStripRegionView::AnimateExpandOnHover(bool expand) {
   is_expanded_on_hover_ = expand;
   ResetExpandOnHoverTimers();
+  // Expanded on hover the strip is drawn at its open width while its state
+  // stays collapsed: the rows take their open form for the overlay and the
+  // rail's again when it goes.
+  ApplyRailForm(!expand);
 
   if (expand) {
     expand_on_hover_start_time_ = base::TimeTicks::Now();
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
index 5178c92c368a7..7c9a3a31023b3 100644
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h
@@ -56,6 +56,7 @@ class Label;
 class ResizeArea;
 class Separator;
 class View;
+class BoxLayout;
 class FlexLayout;
 }  // namespace views
 
@@ -265,6 +266,16 @@ class VerticalTabStripRegionView final
   void OnActiveTabChanged(const tabs::TabInterface* active_tab) override;
 
   raw_ptr<spaces::SpaceSwitcherView> space_switcher_ = nullptr;
+  // The row the downloads button and the switcher share; vertical in the rail.
+  raw_ptr<views::View> bottom_row_ = nullptr;
+  raw_ptr<views::BoxLayout> bottom_row_layout_ = nullptr;
+  // The rail's dress for the rows that have one: icon-only, stacked, centred.
+  // Applied on the collapse state and again by the expand-on-hover animation,
+  // which widens the strip without changing the state (sidebar Y6).
+  void ApplyRailForm(bool rail);
+  // The gutter's worth of interior margin the collapsed strip carries
+  // (sidebar Y6), for the button containers' margins in ApplyRailForm.
+  int rail_left_ = 0;
   // archive A7: the row above the switcher that opens the archived view.
   raw_ptr<views::LabelButton> archived_row_ = nullptr;
   // Stedding (tabs R11-R13): row numbers while Cmd is held. Modifier-only
diff --git a/chrome/browser/ui/views/spaces/space_switcher_view.cc b/chrome/browser/ui/views/spaces/space_switcher_view.cc
index 541a2d3db7575..36547bed21527 100644
--- a/chrome/browser/ui/views/spaces/space_switcher_view.cc
+++ b/chrome/browser/ui/views/spaces/space_switcher_view.cc
@@ -202,12 +202,28 @@ SpaceSwitcherView::SpaceSwitcherView(SpaceModel& model, Browser& browser)
 
 SpaceSwitcherView::~SpaceSwitcherView() = default;
 
+void SpaceSwitcherView::SetCollapsed(bool collapsed) {
+  if (collapsed_ == collapsed) {
+    return;
+  }
+  collapsed_ = collapsed;
+  Rebuild();
+}
+
 void SpaceSwitcherView::Rebuild() {
   if (!icon_row_) {
     return;
   }
   icon_row_->RemoveAllChildViews();
   chips_.clear();
+  if (auto* const row_layout =
+          static_cast<views::FlexLayout*>(icon_row_->GetLayoutManager())) {
+    row_layout->SetOrientation(collapsed_
+                                   ? views::LayoutOrientation::kVertical
+                                   : views::LayoutOrientation::kHorizontal);
+    row_layout->SetInteriorMargin(
+        collapsed_ ? gfx::Insets() : gfx::Insets::VH(0, kRowInset));
+  }
   // The chips are new: a floating name left by one of the old chips (a
   // menu or a drag ended without the pointer leaving it) would stay put.
   HideHoverName();
@@ -216,9 +232,17 @@ void SpaceSwitcherView::Rebuild() {
       views::MinimumFlexSizeRule::kScaleToZero,
       views::MaximumFlexSizeRule::kUnbounded);
   const auto add_spacer = [&] {
+    // No spacers in the rail: the column is one chip wide and the stack
+    // sizes itself.
+    if (collapsed_) {
+      return;
+    }
     icon_row_->AddChildView(std::make_unique<views::View>())
         ->SetProperty(views::kFlexBehaviorKey, stretch);
   };
+  const gfx::Insets chip_margin = collapsed_
+                                      ? gfx::Insets::VH(kChipGap / 2, 0)
+                                      : gfx::Insets::VH(0, kChipGap / 2);
 
   // Arc's row: the chips sit together in the middle with a small, fixed gap,
   // and "+" keeps the right end; the downloads button (the sidebar's, outside
@@ -275,7 +299,7 @@ void SpaceSwitcherView::Rebuild() {
     // LabelButton's default side insets are wider than the chip; without
     // this the label has no width and the glyph never paints.
     button->SetBorder(views::CreateEmptyBorder(gfx::Insets()));
-    button->SetProperty(views::kMarginsKey, gfx::Insets::VH(0, kChipGap / 2));
+    button->SetProperty(views::kMarginsKey, chip_margin);
     button->SetEnabledTextColors(
         SkColorSetA(SK_ColorWHITE, active ? 0xFF : kInactiveAlpha));
     chips_.emplace_back(icon_row_->AddChildView(std::move(button)), space.id());
@@ -297,7 +321,18 @@ void SpaceSwitcherView::Rebuild() {
   add->SetPreferredSize(gfx::Size(kIconSize, kIconSize));
   add->SetTooltipText(u"New Space");
   add->SetBorder(views::CreateEmptyBorder(gfx::Insets()));
+  if (collapsed_) {
+    add->SetProperty(views::kMarginsKey, chip_margin);
+  }
   icon_row_->AddChildView(std::move(add));
+  // The rail's stack is as tall as its chips; the open row is one chip tall.
+  const int rows = static_cast<int>(spaces.size()) + 1;
+  // The open row takes its width from the sidebar's flex; stacked in the
+  // rail it sits in a vertical BoxLayout, which sizes the cross axis from the
+  // preferred size, and a width of 0 left the chips nothing to paint in.
+  SetPreferredSize(gfx::Size(
+      collapsed_ ? kIconSize : 0,
+      collapsed_ ? rows * (kIconSize + kChipGap) + 8 : kRowHeight));
   ApplyOverflow();
 }
 
diff --git a/chrome/browser/ui/views/spaces/space_switcher_view.h b/chrome/browser/ui/views/spaces/space_switcher_view.h
index af8be0589e4c8..765884325e205 100644
--- a/chrome/browser/ui/views/spaces/space_switcher_view.h
+++ b/chrome/browser/ui/views/spaces/space_switcher_view.h
@@ -90,6 +90,11 @@ class SpaceSwitcherView : public views::View,
 
   // tabs R11: the chips show their Ctrl-N chord in place of the glyph while
   // Cmd is held.
+  // The rail (sidebar Y6): one chip wide, so the chips stack vertically with
+  // no spacers and the row is as tall as the stack, instead of a horizontal
+  // row that ran out of the rail and over the page (operator, 2026-09-08).
+  void SetCollapsed(bool collapsed);
+
   void SetNumberBadges(bool showing);
 
   // Opens the Space menu (icon, colour, rename, delete) for `id` at `point`
@@ -148,6 +153,8 @@ class SpaceSwitcherView : public views::View,
   const raw_ref<Browser> browser_;
   raw_ptr<views::Textfield> rename_field_ = nullptr;
   std::optional<SpaceId> renaming_;
+  // In the sidebar's rail the chips stack down a column; see SetCollapsed.
+  bool collapsed_ = false;
   raw_ptr<views::View> icon_row_ = nullptr;
 
   // The chip for each Space, in model order. Parallel to model_->spaces() and
diff --git a/chrome/browser/ui/views/tabs/common/dragged_tabs_container.cc b/chrome/browser/ui/views/tabs/common/dragged_tabs_container.cc
index 0cc5a17a9748f..00e1d8ea7b8e9 100644
--- a/chrome/browser/ui/views/tabs/common/dragged_tabs_container.cc
+++ b/chrome/browser/ui/views/tabs/common/dragged_tabs_container.cc
@@ -5,13 +5,16 @@
 #include "chrome/browser/ui/views/tabs/common/dragged_tabs_container.h"
 
 #include "base/callback_list.h"
+#include "base/feature_list.h"
 #include "base/i18n/rtl.h"
 #include "base/notreached.h"
 #include "base/time/time.h"
 #include "base/types/to_address.h"
 #include "chrome/browser/ui/layout_constants.h"
+#include "chrome/browser/ui/stedding_ui_metrics.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
 #include "chrome/browser/ui/tabs/vertical_tab_strip_state_controller.h"
+#include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/common/tab_drag_handler.h"
 #include "chrome/browser/ui/views/tabs/common/tab_strip_collection_controller.h"
@@ -490,9 +493,16 @@ gfx::Rect DraggedTabsContainer::GetDraggingViewsBoundsAtPointClamped(
   CHECK(scroll_view);
   gfx::Rect clamping_bounds = views::View::ConvertRectToTarget(
       scroll_view, base::to_address(host_view_), scroll_view->GetLocalBounds());
+  // Stedding (sidebar Y8): the rail's rows take the column, so a drag in
+  // the rail clamps to the rail's padding; by the width being drawn.
+  const bool rail = base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+                    scroll_view->width() <=
+                        VerticalTabStripRegionView::kCollapsedWidth;
   clamping_bounds.set_width(
       clamping_bounds.width() -
-      GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding));
+      (rail ? stedding::kRailRowPadding
+            : GetLayoutConstant(
+                  LayoutConstant::kVerticalTabStripHorizontalPadding)));
   bounding_box_for_point.AdjustToFit(clamping_bounds);
 
   return bounding_box_for_point;
diff --git a/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc b/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
index d46c30d859b51..3eefdd2d311b4 100644
--- a/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
+++ b/chrome/browser/ui/views/tabs/common/pinned_tab_container_view.cc
@@ -4,7 +4,9 @@
 
 #include "chrome/browser/ui/views/tabs/common/pinned_tab_container_view.h"
 
+#include "base/feature_list.h"
 #include "chrome/browser/ui/stedding_ui_metrics.h"
+#include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 
 #include "base/i18n/rtl.h"
 #include "chrome/browser/ui/layout_constants.h"
@@ -406,9 +408,17 @@ views::ProposedLayout PinnedTabContainerView::CalculateVerticalLayout(
 
     // Apply horizontal padding immediately at start of collapse animation by
     // including collapsing state.
-    int available_width =
-        size_bounds.width().value() -
+    // Stedding (sidebar Y8): the rail's padding, by the width being laid
+    // out (HANDOFF trap 33). The strip hands this container its width less
+    // the leading padding, so the rail is that plus the padding at most.
+    int horizontal_padding =
         GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
+    if (base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+        size_bounds.width().value() + stedding::kRailRowPadding <=
+            VerticalTabStripRegionView::kCollapsedWidth) {
+      horizontal_padding = stedding::kRailRowPadding;
+    }
+    int available_width = size_bounds.width().value() - horizontal_padding;
 
     // When we are in collapsed state, only one child should be shown per row.
     // During collapse animation and other cases, fit as many as possible.
diff --git a/chrome/browser/ui/views/tabs/common/tab_group_view_layout.cc b/chrome/browser/ui/views/tabs/common/tab_group_view_layout.cc
index 8eefd99313a2c..38e3b8b8767c1 100644
--- a/chrome/browser/ui/views/tabs/common/tab_group_view_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_group_view_layout.cc
@@ -7,8 +7,11 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/feature_list.h"
 #include "chrome/browser/ui/layout_constants.h"
+#include "chrome/browser/ui/stedding_ui_metrics.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
+#include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/common/split_tab_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/common/tab_group_header_view.h"
@@ -75,12 +78,21 @@ views::ProposedLayout TabGroupViewLayout::CalculateVerticalLayout(
 
   // If the tab strip is collapsed then the group line should appear on the
   // leading side of all grouped tabs and the header.
+  // Stedding (sidebar Y8): in the rail the rows take the column and the
+  // group's line sits on its edge; by the width being laid out (trap 33).
+  const bool rail = base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+                    size_bounds.width().is_bounded() &&
+                    size_bounds.width().value() <=
+                        VerticalTabStripRegionView::kCollapsedWidth;
+  const int collapsed_padding =
+      rail ? stedding::kRailRowPadding
+           : GetLayoutConstant(
+                 LayoutConstant::kVerticalTabStripHorizontalPadding);
   if (tab_strip_collapse_state !=
       tabs::VerticalTabStripCollapseState::kExpanded) {
-    group_line_bounds.set_x(kGroupLineCollapsedLeadingPadding);
+    group_line_bounds.set_x(rail ? 0 : kGroupLineCollapsedLeadingPadding);
     group_line_bounds.set_y(height);
-    header_bounds.set_x(
-        GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding));
+    header_bounds.set_x(collapsed_padding);
   }
 
   header_bounds.set_y(height);
@@ -127,8 +139,7 @@ views::ProposedLayout TabGroupViewLayout::CalculateVerticalLayout(
     int child_x = 0;
     if (tab_strip_collapse_state !=
         tabs::VerticalTabStripCollapseState::kExpanded) {
-      child_x =
-          GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
+      child_x = collapsed_padding;
     } else if (!is_focused) {
       child_x = TabGroupView::kTabLeadingPadding;
     }
diff --git a/chrome/browser/ui/views/tabs/common/tab_strip_view.cc b/chrome/browser/ui/views/tabs/common/tab_strip_view.cc
index 61ba0b33d2e6f..25fb9768adab6 100644
--- a/chrome/browser/ui/views/tabs/common/tab_strip_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_strip_view.cc
@@ -554,6 +554,28 @@ void TabStripView::SetCollapsedState(bool is_collapsed) {
   }
 }
 
+void TabStripView::SetRailForm(bool rail) {
+  if (!new_tab_row_ ||
+      !base::FeatureList::IsEnabled(stedding::kArcStyleWindow)) {
+    return;
+  }
+  // The "+ New Tab" row is its plus alone in the rail, centred in the row
+  // and a size up like the rail's favicons; the label and the favicon
+  // column's 8 DIP inset are for the open sidebar. That inset clipped the
+  // plus to 10 DIP in the rail's 26 DIP row (operator, 2026-09-08).
+  new_tab_row_->SetHorizontalAlignment(rail ? gfx::ALIGN_CENTER
+                                            : gfx::ALIGN_LEFT);
+  new_tab_row_->SetText(rail ? std::u16string() : u"New Tab");
+  new_tab_row_->SetBorder(
+      views::CreateEmptyBorder(gfx::Insets::VH(0, rail ? 0 : 8)));
+  new_tab_row_->SetImageModel(
+      views::Button::STATE_NORMAL,
+      ui::ImageModel::FromVectorIcon(
+          vector_icons::kAdd2Icon, kColorTabForegroundInactiveFrameActive,
+          stedding::SidebarFaviconSize() + (rail ? 4 : 0)));
+  InvalidateLayout();
+}
+
 void TabStripView::SetIsAnimatingSize(bool is_animating) {
   for (views::ScrollView* scroll_view :
        {pinned_tabs_scroll_view_, unpinned_tabs_scroll_view_}) {
diff --git a/chrome/browser/ui/views/tabs/common/tab_strip_view.h b/chrome/browser/ui/views/tabs/common/tab_strip_view.h
index 0e39b697f0793..c39974a1c5f7a 100644
--- a/chrome/browser/ui/views/tabs/common/tab_strip_view.h
+++ b/chrome/browser/ui/views/tabs/common/tab_strip_view.h
@@ -74,6 +74,10 @@ class TabStripView final : public views::View, public views::WidgetObserver {
   }
 
   void SetCollapsedState(bool is_collapsed);
+  // sidebar Y8: the "+ New Tab" row's dress for the rail -- its plus alone,
+  // centred and a size up -- or for the open sidebar. The region view calls
+  // this by the width being drawn, not the collapse state (HANDOFF trap 33).
+  void SetRailForm(bool rail);
   void SetIsAnimatingSize(bool is_animating);
 
   bool IsPositionInWindowCaption(const gfx::Point& point);
diff --git a/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc b/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
index a2bd373ab760d..a0e11127dd7c1 100644
--- a/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_strip_view_layout.cc
@@ -13,6 +13,7 @@
 #include "chrome/browser/ui/layout_constants.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
 #include "chrome/browser/ui/views/tabs/common/pinned_tab_container_view.h"
+#include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_strip_view.h"
 #include "chrome/browser/ui/views/tabs/common/unpinned_tab_container_view.h"
 #include "ui/views/controls/scroll_view.h"
@@ -168,8 +169,15 @@ views::ProposedLayout TabStripViewLayout::CalculateVerticalLayout(
     return layouts;
   }
 
+  // Stedding (sidebar Y8): in the rail the containers take the column, 3 DIP
+  // from the strip's edges; by the width being laid out (HANDOFF trap 33).
+  const bool rail = base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+                    size_bounds.width().value() <=
+                        VerticalTabStripRegionView::kCollapsedWidth;
   const int region_horizontal_padding =
-      GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
+      rail ? stedding::kRailRowPadding
+           : GetLayoutConstant(
+                 LayoutConstant::kVerticalTabStripHorizontalPadding);
 
   const int region_vertical_padding = GetLayoutConstant(
       LayoutConstant::kVerticalTabStripCollapsedVerticalPadding);
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.cc b/chrome/browser/ui/views/tabs/common/tab_view.cc
index b65d35efe3ce4..6294b0cf6f675 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view.cc
@@ -1453,6 +1453,14 @@ int TabView::UncollapsedMinWidth() {
 
 // static
 int TabView::CollapsedWidth() {
+  // Stedding (sidebar Y8): the rail's rows take the column. The strip is
+  // the rail less the card gutter's worth of interior margin, and a row is
+  // that less the rail's padding on each side: 44 DIP. IsInExpandOnHover
+  // reads this, so it must be the width the containers hand a rail row.
+  if (base::FeatureList::IsEnabled(stedding::kArcStyleWindow)) {
+    return VerticalTabStripRegionView::kCollapsedWidth -
+           stedding::kCardGutter.Get() - 2 * stedding::kRailRowPadding;
+  }
   return VerticalTabStripRegionView::kCollapsedWidth -
          2 * GetLayoutConstant(
                  LayoutConstant::kVerticalTabStripHorizontalPadding);
diff --git a/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc b/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
index 353ac4d8e1784..50aadd824adf0 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view_vertical_layout.cc
@@ -118,9 +118,18 @@ views::ProposedLayout TabViewVerticalLayout::CalculateProposedLayout(
       continue;
     }
     if (is_child_visible && can_render_child) {
-      layouts.child_layouts.emplace_back(
-          child.view.get(), is_child_visible,
-          GetChildBounds(bounds_remaining, child, is_centered));
+      gfx::Rect child_bounds =
+          GetChildBounds(bounds_remaining, child, is_centered);
+      if (stedding_rows && is_centered && TabView().collapsed_ &&
+          child.view == TabView().icon_) {
+        // The rail's favicon, a size up (sidebar Y6; operator, 2026-09-08).
+        const int rail_icon = IconDesignWidth() + 4;
+        child_bounds = gfx::Rect((width - rail_icon) / 2,
+                                 (height - rail_icon) / 2, rail_icon,
+                                 rail_icon);
+      }
+      layouts.child_layouts.emplace_back(child.view.get(), is_child_visible,
+                                         child_bounds);
 
       if (!is_centered) {
         bounds_remaining.Inset(
diff --git a/chrome/browser/ui/views/tabs/common/unpinned_tab_container_view_layout.cc b/chrome/browser/ui/views/tabs/common/unpinned_tab_container_view_layout.cc
index b4e115cd1db4f..de3586cba7702 100644
--- a/chrome/browser/ui/views/tabs/common/unpinned_tab_container_view_layout.cc
+++ b/chrome/browser/ui/views/tabs/common/unpinned_tab_container_view_layout.cc
@@ -7,9 +7,12 @@
 #include <algorithm>
 #include <vector>
 
+#include "base/feature_list.h"
 #include "base/numerics/safe_conversions.h"
 #include "chrome/browser/ui/layout_constants.h"
+#include "chrome/browser/ui/stedding_ui_metrics.h"
 #include "chrome/browser/ui/tabs/tab_style.h"
+#include "chrome/browser/ui/views/frame/vertical_tab_strip_region_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_collection_node.h"
 #include "chrome/browser/ui/views/tabs/common/tab_group_view.h"
 #include "chrome/browser/ui/views/tabs/common/tab_strip_collection_controller.h"
@@ -171,8 +174,18 @@ views::ProposedLayout UnpinnedTabContainerViewLayout::CalculateVerticalLayout(
   int dragged_view_bottom = 0;
   auto collapse_state = tab_container_view->GetTabStripCollapseState();
 
+  // Stedding (sidebar Y8): in the rail the rows take the column, 3 DIP from
+  // the strip's edges. Keyed on the width being laid out, not the collapse
+  // state (HANDOFF trap 33): expanded on hover the state stays collapsed
+  // while the rows are at the open width, with the open sidebar's padding.
+  const bool rail = base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+                    size_bounds.width().is_bounded() &&
+                    size_bounds.width().value() <=
+                        VerticalTabStripRegionView::kCollapsedWidth;
   const int horizontal_padding =
-      GetLayoutConstant(LayoutConstant::kVerticalTabStripHorizontalPadding);
+      rail ? stedding::kRailRowPadding
+           : GetLayoutConstant(
+                 LayoutConstant::kVerticalTabStripHorizontalPadding);
 
   // Stedding: the boundary between the Space-pinned run and the rest goes
   // after the last child the predicate accepts (or at the top when there is
diff --git a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.cc b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.cc
index 2d814fcdbbc3c..6bf84937f0a60 100644
--- a/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.cc
+++ b/chrome/browser/ui/views/tabs/vertical/vertical_tab_strip_top_container.cc
@@ -130,9 +130,18 @@ views::ProposedLayout VerticalTabStripTopContainer::CalculateProposedLayout(
       current_y = std::max(current_y, CaptionClearance());
     }
 
+    // Stedding (sidebar Y6): the rail's buttons centre in the rail like its
+    // rows, rather than hugging its left edge in a column three times their
+    // width (operator, 2026-09-08). By the width actually available, not the
+    // collapse state: expanded on hover the state is still collapsed while
+    // the container is the open sidebar's width, and the toggle centred in
+    // that.
+    const bool centre_in_rail =
+        base::FeatureList::IsEnabled(stedding::kArcStyleWindow) &&
+        available_width <= VerticalTabStripRegionView::kCollapsedWidth;
     if (unfocus_button_ && unfocus_button_->GetVisible()) {
       const gfx::Size pref_size = unfocus_button_->GetPreferredSize();
-      int x = is_collapsed
+      int x = (is_collapsed && !centre_in_rail)
                   ? 0
                   : std::max(0, (host_size.width() - pref_size.width()) / 2);
       gfx::Rect bounds(x, current_y, pref_size.width(), pref_size.height());
@@ -145,7 +154,7 @@ views::ProposedLayout VerticalTabStripTopContainer::CalculateProposedLayout(
 
     if (collapse_button_ && collapse_button_->GetVisible()) {
       const gfx::Size pref_size = collapse_button_->GetPreferredSize();
-      int x = is_collapsed
+      int x = (is_collapsed && !centre_in_rail)
                   ? 0
                   : std::max(0, (host_size.width() - pref_size.width()) / 2);
       gfx::Rect bounds(x, current_y, pref_size.width(), pref_size.height());
diff --git a/chrome/browser/ui/views/toolbar/toolbar_view.cc b/chrome/browser/ui/views/toolbar/toolbar_view.cc
index 413666b18a9e8..d9df2fe3580d1 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -1470,19 +1470,37 @@ void ToolbarView::Layout(PassKey) {
   // centre of the space between the button clusters -- the leading cluster
   // is the wider, so "between" sat the bar 19 DIP right of the page's
   // middle. It only moves as far as the clusters allow.
+  // With no width to spare the layout drops the spacers -- hidden, at the
+  // origin -- and their bounds mean nothing; the field then keeps the
+  // layout's own position. Reading them anyway clamped the field to x 0,
+  // over the back, forward and reload buttons, in a window under about
+  // 1000 DIP with the sidebar open (2026-09-08).
   if (display_mode_ == DisplayMode::kNormal && location_bar_view_ &&
-      stedding_left_spacer_ && stedding_right_spacer_) {
+      stedding_left_spacer_ && stedding_right_spacer_ &&
+      stedding_left_spacer_->GetVisible() &&
+      stedding_right_spacer_->GetVisible()) {
     const gfx::Rect bar = location_bar_view_->bounds();
     const int left_limit = stedding_left_spacer_->x();
     const int right_limit = stedding_right_spacer_->bounds().right();
-    const int x = std::clamp((width() - bar.width()) / 2, left_limit,
-                             std::max(left_limit, right_limit - bar.width()));
+    // On the bar's centre, which on Windows runs on past this view under
+    // the caption buttons (toolbar T19); the clusters still bound it.
+    const int x = std::clamp(
+        (width() + stedding_trailing_reserve_ - bar.width()) / 2, left_limit,
+        std::max(left_limit, right_limit - bar.width()));
     if (x != bar.x()) {
       location_bar_view_->SetX(x);
     }
   }
 }
 
+void ToolbarView::SetSteddingTrailingReserve(int reserve) {
+  if (stedding_trailing_reserve_ == reserve) {
+    return;
+  }
+  stedding_trailing_reserve_ = reserve;
+  InvalidateLayout();
+}
+
 void ToolbarView::OnThemeChanged() {
   views::AccessiblePaneView::OnThemeChanged();
   if (!initialized_) {
diff --git a/chrome/browser/ui/views/toolbar/toolbar_view.h b/chrome/browser/ui/views/toolbar/toolbar_view.h
index 23a055ace8edd..c1fe9c8165bb7 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_view.h
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.h
@@ -233,6 +233,11 @@ class ToolbarView : public views::AccessiblePaneView,
   gfx::Size GetMinimumSize() const override;
   void Layout(PassKey) override;
   void OnThemeChanged() override;
+
+  // Stedding (toolbar T19): how far the bar runs on past this view's right
+  // edge, under Windows' caption buttons. The field centres on the bar
+  // (T18), so the centre is computed over the width plus this.
+  void SetSteddingTrailingReserve(int reserve);
   bool AcceleratorPressed(const ui::Accelerator& acc) override;
   void ChildPreferredSizeChanged(views::View* child) override;
   void ChildVisibilityChanged(View* child) override;
@@ -385,6 +390,7 @@ class ToolbarView : public views::AccessiblePaneView,
   // button clusters they mark.
   raw_ptr<views::View> stedding_left_spacer_ = nullptr;
   raw_ptr<views::View> stedding_right_spacer_ = nullptr;
+  int stedding_trailing_reserve_ = 0;
 
   // An alias for `location_bar_view_` or `toolbar_webview_->GetLocationBar()`.
   raw_ptr<LocationBar> location_bar_ = nullptr;
