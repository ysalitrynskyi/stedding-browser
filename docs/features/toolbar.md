# Feature: Toolbar and page bar

Status: **T1–T8, T10–T12, T14–T18 built**; **T13 planned (D10), T9 dropped** (round 6, `docs/ROUND6-PLAN.md` R6-25). T15–T18 are round 7 (`docs/ARC-ROUND2.md`, 2026-09-05).
Owner docs: `docs/UI-SPEC.md`. Patches: `0002` (layout), `0007` (colours), `0037` (round 7: T15–T18).

The address row is the top of the page card, not a strip above it: no gap, no hairline,
the same gutter as the card on its three free sides (6 DIP, the `card_gutter` parameter; 8 until 2026-09-05, when the top had none), and the page's own colour when the page declares
one. Arc's window reads the same way.

## Behaviours

| Id | Behaviour | Test | State |
|---|---|---|---|
| T1 | The content card starts directly under the address row: no gap; the one hairline between them is T15's. | `tooling/probes/window.json` (content top at the toolbar's bottom) | built |
| T2 | The row keeps the card's gutters (`card_gutter`, 6 DIP) at the sides and sits the same gutter below the window's top edge, so the omnibox and the page share edges and the distance from the window's top to the row equals the right and bottom borders (operator, 2026-09-05). | probes: toolbar row left/right/top | built |
| T3 | A page whose colour — its `theme-color`, or, without one, its own background (`WebContents::GetBackgroundColor`, the way Safari tints its bar) — sits on the same side of the contrast line as the theme (dark in dark mode, light in light) colours the row that colour itself (T15; blended 85% over the ground until 2026-09-05), so the row reads as the top of the page: white for chrome://settings or Wikipedia in light, GitHub's dark in dark. | live: `tooling/drive` on github.com (dark) vs example.com (none); `tooling/probes/window.json` keeps example.com's row on the ground | built |
| T4 | A page whose colour is on the other side of the contrast line (a dark page in light mode, a white page in dark) colours the row all the same, and the row's icons and text flip for contrast (T7), as Safari does; until 2026-09-04 such a row stayed on the ground. A page with no colour leaves the row on the ground (probes 5 and 12 on example.com). | probes 5 and 12 on example.com; live: github.com in light mode, the row dark and the back arrow light (a probe on the arrow) | built |
| T5 | The omnibox is text on the bar, with no background of its own — a fill drew a box whose left edge sat against the address, so it went (operator round 7) — and a quiet 8% tint of the icon colour under the pointer to show where to click. The field is 560 DIP (`location_bar_width`) where the toolbar has the room and gives way when it does not: a narrow window or a row of extension actions shrinks it, never below its own minimum. The address is one point smaller than Chromium's. The page-info chip ("Stedding" on chrome:// pages) keeps 21 DIP for its 16 DIP icon inside the 25 DIP location bar (found clipped 2026-09-04). | `AddressRowWidthTest.FieldWidthGivesWayWhenTheRowIsTight`; capture: `ui-current` | built |
| T6 | In macOS immersive fullscreen the row moves into the overlay; the sidebar's top row stays as compact as outside fullscreen. The sidebar cannot reach the overlay's 33 DIP (platform: the window's content view starts below it). | live capture (⌃⌘F) | built |
| T7 | The row's icon and text colours follow the page colour through the ColorProvider: `PageBarColorSupplier` rides on the window's `ColorProviderKey` as its app controller (the slot web apps use for their theme colour) and remaps the toolbar's icon, text and omnibox ids to the colour with most contrast against the painted bar; the sidebar's buttons use the tab-strip control ids and keep theirs. A page-colour change re-themes the window the way a web app's theme-colour change does. | live: github.com (dark) in light mode and example.com; probe on the back arrow's pixel, dark and light | built |
| T8 | With the sidebar collapsed (⌘S) and the setting on, the address row hides: `BrowserView::IsToolbarVisible` answers no, the top container keeps only the card gutter, the content card takes the full height with its radius and gutters, and `PaintPageBar` paints nothing. Immersive fullscreen is unchanged (T6). | `AddressRowRulesTest.HiddenWithTheSidebarUnlessLocalhostOrOverridden`; live: `w3_row_collapsed` (dark), `w3_row_collapsed_light` (the card takes the full height with its gutters; no bar) | built |
| T9 | Resting the pointer at the top floats the row as a pill; ⌘L summons it. | none yet | gap · dropped: ⌘L opens the command bar (commandbar K12); no floating pill (`docs/ROUND6-PLAN.md` D2) |
| T10 | ⇧⌘D toggles the row for the window on its own: shown while the sidebar is collapsed, or hidden while it is expanded. The override clears when the sidebar's state changes, so ⌘S behaves as the setting says again. Bookmark All Tabs loses the chord (its menu item stays). | `AddressRowTest.ToggleOverridesUntilTheSidebarChanges`; live: `w3_row_toggled` (⇧⌘D with the sidebar collapsed brings the row back) | built |
| T11 | A localhost page always shows the row (Arc's Developer Mode). | `AddressRowRulesTest.HiddenWithTheSidebarUnlessLocalhostOrOverridden`; live: `w3_row_localhost` (a localhost page keeps the row while collapsed) | built |
| T12 | Setting off keeps the row whatever the sidebar does (today's behaviour); ⇧⌘D still works. | `SteddingPrefsTest.EveryPreferenceRegistersAndDefaultsToOn` (the default); `AddressRowRulesTest.HiddenWithTheSidebarUnlessLocalhostOrOverridden`; the toggle: `w3_row_settings` | built |
| T13 | With the row hidden, extension actions, the media button and page info live in the command bar's actions mode; ⌘E cycles extension actions; page info opens from the address text in the bar. | TBD | planned · D10, the second half |
| T14 | The bottom-left downloads button shows Chromium's progress ring and the download-started animation; the toolbar's own button never appears at the top right during a download (backlog S-41). | live: `w2_download_ring` (a 24 MB download from a local server at about 1 MB/s; the sidebar's button shows the ring, the toolbar shows no button). | built |
| T15 | The row is the page's colour exactly, not a blend over the window ground (a white page's row came out grey, operator round 7); one hairline, a tenth of the contrast colour, marks where the row meets the page. | `PageBarColorSupplierTest.IconsContrastWithThePageColour` (the separator id is the page's own white); probe: `tooling/probes/window.json` (the row's pixel is the page's) | built |
| T16 | The page starts square under the row: with the row showing, the card's rounded top belongs to the row, and a page whose header is not its theme colour no longer shows the row's colour through rounded top corners; with the row hidden (T8) the page is the whole card, four corners rounded. | live: `r7_dark_header` (a white theme-colour page with a dark header: the header's top corners are square); `w3_row_collapsed` still rounds all four | built |
| T17 | The star and the row's other page actions take the colour with most contrast against the bar, as the toolbar's icons do (they kept the theme's colour and were faint on a light bar in dark mode). | `PageBarColorSupplierTest.IconsContrastWithThePageColour` (`kColorOmniboxActionIcon` equals the button icon colour); live: `r7_theme_white` (the star dark on the white row) | built |
| T19 | On Windows the caption buttons are frame-owned controls at the top right, over the row's column, and the row stops short of them (the frame's trailing exclusion); the card's exposed top-right corner rounds; with the row hidden (T8) the top container keeps the caption strip's height so the card never covers the buttons. macOS is unchanged: its leading exclusion is over the sidebar's column. | none; live: `w8_base`, `w8_collapsed` (Windows, 2026-09-08) | partial · no unit test yet (S-49) |
| T20 | The focused location bar and the results dropdown take the page's colour, their text, icons and selection the bar's contrast colour; they took the theme's dropdown colour, near-black on a white page's row, until 2026-09-08. | none; live: `w8_focus` (Windows) | partial · no unit test yet (S-49) |
| T21 | The row keeps its colour across a reload: a new Page reports no colour of its own and the WebContents announces a background only when it differs from the last one it sent, so the controller keeps the colour while the page loads and re-reads the Page at first paint and at load end. | none; live: `w8_reload` (Windows) | partial · no unit test yet (S-49) |
| T22 | With the frame's own buttons over the row's column -- Windows' caption buttons, the frame's trailing exclusion -- the row stays whatever the sidebar does: the strip is there anyway, and hiding the row left an empty band and no address bar. ⇧⌘D's answer (T10) still wins. macOS is unchanged (T8). | `AddressRowRulesTest.KeptUnderTheCaptionButtons`; live: `w8_rail` (Windows, 2026-09-08) | partial · the test is written, not yet run on a Mac (S-49) |
| T23 | A bubble asked for before the window is shown while the row is hidden -- the crashed-session "Restore pages?" bubble at startup -- anchors to the top container: the app menu button is not drawn, and the fallback anchor is a tracked element the tracker does not know until the widget is visible. A profile killed while the sidebar was collapsed crashed on every launch until 2026-09-08 (`Check failed: element`, `AppMenuButton::GetAnchor`). | none; live: the round's crashing test profile opens with the bubble (Windows, 2026-09-08) | partial · no unit test yet (S-49) |
| T18 | The field sits on the row's centre rather than between the button clusters (the leading cluster is wider, so "between" sat it 19 DIP right of the page's middle), and inside it the chip and the address centre in the space left of the page actions, which keep the field's right edge (Safari's compact bar); while the address is being edited, or fills the field, Chromium's own left-aligned layout stands. Three cuts the same evening did not survive the operator's eye: a field sized to a predicted content width elided "github.com" to "githu…", a cluster gathered mid-row read as a too-short field, and a tinted pill read as a box against the address. | live: `ui-current` (the address centred, the star at the field's edge), `r7_theme_white` (a long address filling the field) | built |
